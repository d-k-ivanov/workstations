---
- name: Install Input Device Software
  ansible.builtin.apt:
    name:
      - solaar    # Linux device manager for Logitech's Unifying Receiver peripherals
    install_recommends: false
  register: task_result
  until: task_result is success
  retries: 10
  delay: 2
